export {
  computeNullifier,
  faceEmbeddingToKey,
  cedulaToBigInt,
  fechaToBigInt,
  generateProof,
  verifyProof,
  serializeProof,
  deserializeProof,
} from "./prover.js";

export type { ZKProofInput, ZKProof, ZKVerifyResult } from "./prover.js";
